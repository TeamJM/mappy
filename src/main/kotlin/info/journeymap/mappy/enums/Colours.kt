package info.journeymap.mappy.enums

/**
 * An enum representing each type of colour that may be configured, for easier reference.
 *
 * @param value A human-readable representation of the given colour.
 */
enum class Colours(val value: String) {
    RED("red"),
    GREEN("green"),
    BLURPLE("blurple")
}
